"""Public API for shared schemas — import from here, not from submodules."""

from shared.schemas.lead import AreaInteresse, LeadCreate, LeadResponse

__all__ = ["AreaInteresse", "LeadCreate", "LeadResponse"]
